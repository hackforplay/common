{
  "name": "@hackforplay/monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@11.8.0",
  "scripts": {
    "build": "turbo run build",
    "test": "npx prettier -c . && turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "2.8.8",
    "turbo": "2.8.2"
  }
}
